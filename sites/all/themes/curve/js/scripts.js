/* ======================== */
/* --- scripts.js --------- */
/* ------------------------ */

(function ($) {
   
	$(document).ready(function() {

		/*
		// cache flip function
		function contentFlip(element_rep, old_val, new_val) {
			$(element_rep).each(function() {
				image_src = $('img', this).attr('src');
				new_src = image_src.replace(old_val, new_val);
				$('img', this).attr('src', new_src);
			});
		}

		// set the initial values
		detector = $('body');
		compareWidth = detector.width();
		
		$(window).resize(function() {		
			if(detector.width()!= compareWidth) {
				compareWidth = detector.width();
				elementCheck();
			}
		});
	
		function elementCheck() {
			if(detector.width() < 640) {
				contentFlip('ul#flexslider_views_slideshow_home_case_study_slideshow-block li.flexslider_views_slideshow_slide', '/styles/case_study_teaser_main/', '/styles/case_study_teaser_mobile/');
				//$('ul.flexslider-views-slideshow-main-frame li.flexslider_views_slideshow_slide').css('border', '1px solid #000');
			} else {
				contentFlip('ul.flexslider-views-slideshow-main-frame li.flexslider_views_slideshow_slide', '/styles/case_study_teaser_mobile/', '/styles/case_study_teaser_main/');
				//$('ul.flexslider-views-slideshow-main-frame li.flexslider_views_slideshow_slide').css('border', '1px solid #f00');
			}
		}
		
		elementCheck();
		*/
		
	});

} )(jQuery);